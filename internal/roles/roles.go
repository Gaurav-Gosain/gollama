package roles

// The different roles that can be assigned to a chat messages
// (according to the Ollama API)
const (
	SYSTEM    string = "system"
	USER      string = "user"
	ASSISTANT string = "assistant"
)
